# HANDOFF: ветка `studio/pipeline-planner`

Файл append-only (Ц6). Дописывать снизу, не править написанное.

## DEBT(2026-09-02): хэндоф заведён не первым действием сессии

Ц6 требует открыть `HANDOFF_<ветка>.md` первым действием. Ветка отходила от
чужой головы (`72a5ec6` основного дерева), своего хэндофа у неё не было, а
чужие править нельзя (Ц2) — файл заведён в конце работы, из полного контекста,
а не из остатков. Что потеряно: пошаговый след решений по ходу. Что нет:
все числа ниже сняты прогонами и лежат файлами в репозитории.

## 2026-09-02. Откуда ветка и что в ней

Ветка `studio/pipeline-planner` отошла от `72a5ec6` (голова основного дерева
`/home/user/lipsync-templates`), потому что у выданного worktree
(`964844f`, `origin/main`) каталога `studio/` нет вовсе.

Задача владельца дословно: «Бот помогает собирать пайплайн на основе готового
креатива и с нуля». Половины были обе, моста не было.

## Что измерено ДО правки (П1, счётчик раньше ручки)

`scripts/planner_baseline.py`, восемь настоящих брифов
(`studio/fixtures/planner_briefs.jsonl`), снимок прогона —
`studio/fixtures/planner_baseline_2026-09-02.txt`:

    брифов в файле 8, разобрано 8
    шагов плана ожидается 10, собрано репозиторием 0
    негативных контролей 2, на скольких поиск фактов всё равно ответил: 1

Второе число важнее первого. Единственный существовавший мост «слова задачи ->
база» — `studio/factindex.py`, поданный брифом целиком — отвечает НА ЧТО
УГОДНО и отвечает не тем:

* «оживить фото клиента, 10 секунд, он говорит мою озвучку» -> 8 фактов о
  8 моделях, и все картиночные: `sdxl-turbo`, `krea-2`,
  `stable-diffusion-3-medium-diffusers`, `sulphur-2-base`. Липсинк не делает
  ни одна;
* «пайплайн, управляющий промышленным манипулятором на складе» -> исход
  `pass`, факт про `indextts-2`;
* «заменить персонажа в готовом ролике, сохранив цветокор и освещение» ->
  НОЛЬ находок, при том что в базе лежит `wan-animate-replace` дословно об
  этом. Бриф русский, база английская — это уже было ИЗМЕРЕНО и записано в
  `studio/factaxis.py` (русских значений 252 из 1696).

Отсюда устройство модуля: кандидат отбирается не весом поиска, а ЯКОРЕМ —
термином, которым операцию называет сама база. Порог крутить бесполезно,
`factaxis` уже измерил, что веса верных и ложных подборов перемешаны.

## Что сделано

* `studio/planner.py` — словарь из семи операций студии, вывод шагов из брифа,
  подбор кандидатов якорями через тот же `FactIndex` (второго поиска нет, Е1),
  сборка `pipeline.Pipeline` и прогон через `pipeline.pipeline_report`
  (второго валидатора нет);
* `studio/tests/test_planner.py` — 29 тестов, сети нет, база синтетическая;
* `scripts/check_planner.py` — гейт на восьми брифах живой базы;
* `scripts/planner_baseline.py` — замер «как было», не импортирует планировщик;
* `scripts/mutate_planner.py` — таблица мутаций Т1;
* `studio/mcp/server.py` — инструмент `plan_pipeline`, шапка 14 -> 15,
  `scripts/check` обновлён тем же коммитом.

## Числа после правки

`bash scripts/check` — EXIT=0. Раздел планировщика:

    брифов в файле 8, разобрано 8
    молчал на 2, собрал план на 6; исходов различено 2
    кандидатов выбрано 10, из них без применимости 4 (все с пометкой: True),
    без доказательства 0

## Т1: мутация, и что она нашла

`scripts/mutate_planner.py`, снимок —
`studio/fixtures/planner_mutation_2026-09-02.txt`.

Первый прогон: 17 мутантов, ПРОМОЛЧАЛИ НА 5. Две молчанки были находками:

1. `APPLICABILITY_FIRST = True` — флаг, за которым не стояло ветвления:
   следующий ключ порядка (`-c.applicability`) давал ровно тот же результат.
   Константа УДАЛЕНА, а не покрыта тестом (Е1: второй способ узнать известное);
2. `CANDIDATES_SHOWN` и `EVIDENCE_SHOWN` — границы того, сколько доходит до
   читателя, и их не видел ни один тест. Заведён класс `ГраницыПечати`.

После правок: 18 мутантов, промолчали на 0.

## Границы прибора, записанные вслух (И6)

* **Исхода `годно` на живой базе нет ни у одного из восьми брифов.** Причина
  измерена, а не угадана: `factaxis.step_verdict` ставит `годно` только там,
  где есть строка ПРИМЕНИМОСТИ, относящаяся к требованию, и она не
  отрицательная. В живой базе применимость почти всегда записана атрибутом из
  `CONTRA_ATTRIBUTES` (`failure_mode`, `metric_blind_spot`). Ветка `pass`
  достижима и сторожится тестом на синтетическом наборе;
* **якорь ловит только то, что база написала словами операции.** Модель,
  делающая липсинк, но без якорного слова в своих строках, в кандидаты не
  попадёт. Видно это как «кандидатов нет», а не как уверенный план;
* **якоря t2v на живой базе тонкие**: пять имён, и первым идёт workflow с
  Civitai. Настоящие t2v-модели (kling, sora, veo, ltx) словом `t2v` в базе
  не помечены. Это цена узкого якоря, и она печатается в выдаче — у
  кандидата стоит его единственная строка с датой 2025-06-25;
* **порядок кандидатов поднимает того, о ком записана плохая новость,** выше
  того, о ком не записано ничего. Это выбрано сознательно: валидатор назовёт
  класс и скажет «не годно», а молчание базы читатель не отличил бы от
  «годно».

## Что не сделано и куда смотреть дальше

* мост между русским требованием и английской базой так и не построен — он
  обойдён якорями, а не решён. Требование шага написано двуязычно (русская
  фраза плюс английские термины) именно поэтому;
* `звук_фон` стоит в словаре операций, зная, что база о нём молчит: якоря
  `foley`/`sound-effects`/`sfx` дают 0 утверждений из 1776. Это негативный
  контроль в живом словаре, а не забытая работа;
* бюджет шага планировщик принимает и передаёт валидатору, но ни один из
  восьми контрольных брифов его не называет — ветка класса `цена` на живой
  базе через планировщик не проверена.

## 2026-09-02, второй заход. Цена и бюджет: три дефекта на главном вопросе владельца

Ветка была слита (`a9e77b2`), координатор вернул два дефекта, найденных
чтением выдачи (П3). Разбор нашёл третий, который их и объяснял.

### Как воспроизводилось (И2)

    $ python -c "from studio import planner; \
        print(planner.render(planner.plan('нужен липсинк на готовое видео, бюджет 0.5 доллара')))"
    шаг липсинк: wav2lip — применимость измерена: 2 строк(и)
        цена: цена не записана
        ещё кандидаты (15 всего): latentsync (применимость измерена: 1 строк(и)), ...

### Три дефекта, а не два

1. **Бюджет, названный словами, не разбирался вовсе.** `plan()` принимал
   `budget_usd` параметром и в бриф не смотрел.
2. **Цена не участвовала в порядке кандидатов** даже при бюджете-параметре.
3. **Цена считалась ПО ЯКОРНЫМ строкам модели, а не по всем** — и это объясняло
   первые два. ИЗМЕРЕНО:

       якорных строк latentsync: 3 ['degrades_when', 'degrades_when', 'metric_blind_spot']
       всех строк latentsync:   10 ['price', 'loop_mode', 'failure_mode', ...]
       цена по якорным: цена не записана
       цена по всем:    цена не записана (несравнимых ценовых строк 1)

   Якорь отвечает на вопрос «эта ли модель делает эту работу», а не «сколько
   она стоит». Цена была невидима планировщику целиком.

### Что сделано

* `budget_from(brief)` — потолок из фразы заказчика, ТРИ исхода: разобран /
  «сказано, но числа с единицей нет» / «не назван». Единица обязана быть
  названа: «бюджет 50» -> третий исход, а не пятьдесят долларов;
* `DEFAULT_PER = ("run", "generation")` — «за что», когда заказчик не сказал.
  Выведено не из вкуса, а из того, куда уходит число: `pipeline.Step.budget_usd`
  документирован как бюджет ШАГА. Цена в другом «за что» становится
  НЕСРАВНИМОЙ, а не подставляется в сравнение;
* `price_stance` — ЧЕТЫРЕ положения (`PRICE_ORDER`), и четыре, а не три:
  «цена записана, но с бюджетом несравнима» отделено от «цены нет ни строки».
  Слияние этих двух и было вторым дефектом;
* цена стала СТАРШИМ ключом отбора — но только при названном потолке;
* `price_facts` — имена ценовых атрибутов берутся у `studio/selfrag/attrfamily.py`
  (Е1), а не подстрокой. ИЗМЕРЕНО: подстрочный фильтр валидатора пропускает 23
  имени, из них `price_relative`, `storage_cost` и `search_grounding_price`
  ценой модели не являются;
* `why_not` — «почему выбран этот, а не сосед»: ПЕРВОЕ поле ключа, которым они
  разошлись, посчитанное из того же кортежа, по которому произошёл отбор (Е2);
* цена стала считаться по всем строкам модели; строки группируются одним
  проходом (`fold`), иначе шаг липсинка рос с 9 до 82 мс.

### Числа после починки

    $ python -c "... plan('нужен липсинк на готовое видео, бюджет 0.5 доллара') ..."
    бюджет: 0.5 usd [годно] — потолок 0.5 usd; «за что» заказчик НЕ назвал;
            принято «за прогон шага» (run, generation) ...
    шаг липсинк: latentsync — применимость измерена: 1 строк(и)
        цена: цена не записана (несравнимых ценовых строк 1) —
              цена записана, но с бюджетом несравнима: ценовых строк 1,
              сравнимых с потолком 0 (price=$0.2 for videos up to 40 seconds...)
        почему не сосед: выбран latentsync, а не sora-2:
              разошлись по «строк применимости» (1 против 0)

Негативный контроль (тот же бриф без слова «бюджет»): потолок «не назван»,
цена не решает ничего, порядок возвращается к доводам — выбран wav2lip.

Гейт: 10 брифов, шагов с вычитанным потолком 1, нарушений порядка по цене 0,
строк «почему не сосед» 12. Тестов 49. `bash scripts/check` EXIT=0.

Мутация Т1: 28 мутантов (было 18), промолчали на 0. Первый прогон второй серии
показал `ЗДОРОВЫЙ | тесты rc=1` — таблица строилась поверх красного, потому что
в наборе стало 10 брифов, а литерал в тесте остался 8. Строка «ЗДОРОВЫЙ» в
скрипте существует ровно для этого.

### Границы, дописанные к прежним (И6)

* «за что», принятое по умолчанию, — решение, а не измерение. На живой базе оно
  делает несравнимыми цены за секунду и за 1000 знаков; это видно в выдаче
  строкой у каждого кандидата, но означает, что потолок кусается редко:
  из 10 контрольных брифов потолок вычитан на одном, и ни на одном шаге не
  сработало положение «дороже бюджета». Класс `цена` валидатора через
  планировщик по-прежнему не проверен на живой базе;
* цена берётся по ВСЕМ строкам модели, а применимость — по якорным. Это
  осознанная асимметрия: цена не зависит от того, ту ли работу модель делает,
  а применимость зависит.

## 2026-09-02, третий заход. Проверенный, которого цена отодвинула

Слито `49e0a58`. Координатор нашёл чтением выдачи (П3) узкий, но злой случай.

### Как воспроизводилось (И2)

    $ plan('нужен липсинк на готовое видео, бюджет $0.05 за секунду')
    выбран: kling-video-lipsync-audio-to-video | применимость не измерена | 0.014 usd/s
      альт: kling-video-lipsync-text-to-video  | применимость не измерена | 0.014 usd/s
      альт: pixverse-lipsync                   | применимость не измерена | 0.04  usd/s
    найдено кандидатов: 28

Где стояли проверенные (замерено прямым обходом `candidates_for`):

    # 3 latentsync     прим=1  цена записана, но с бюджетом несравнима
    #10 wav2lip        прим=2  цена не записана
    #11 infinitetalk   прим=1  цена не записана
    #12 omnihuman-1.5  прим=1  цена не записана

Каждая напечатанная строка — правда. Неверен ЗАГОЛОВОК: читатель видит три
недоказанные модели и не получает ни одного признака, что доказанная есть.
Порядок при этом верен и НЕ менялся: потолок назвал человек.

### Что сделано

Одна строка на шаг (`rival_line`), и положений в ней ТРИ, а не два:

* выбранный сам проверен — строки НЕТ (была бы шумом);
* выбранный не проверен, проверенный есть — `RIVAL_MARK` с именем сильнейшего
  проверенного и с тем, ЧЕМ он проиграл (берётся у `why_not`, то есть из того
  же ключа, по которому произошёл отбор, — Е2);
* проверенных нет ни одного — `NO_RIVAL_MARK`, ДРУГИМИ словами. Одно молчание
  на два разных положения дел и было бы тем самым дефектом.

Печатается СРАЗУ под именем модели, до цены и до доказательств.

`proven()` выбирает сильнейшего проверенного по доводам БЕЗ ценового ключа:
иначе вытесненным назывался бы тот же, кого цена и подняла наверх.

### Почему не понадобился флаг «цена переставила порядок»

Ветка «выбран непроверенный, а проверенный есть» достижима ТОЛЬКО при
названном потолке, и это следует из самого ключа: без потолка ранг цены у всех
нулевой, старшим ключом становится `-applicability`, и выбранным оказывается
кандидат с наибольшим числом строк применимости. Отдельный флаг был бы вторым
способом узнать известное (Е1) — как `APPLICABILITY_FIRST`, который мутация уже
убила однажды. Сторожится тестами `test_без_потолка_вытеснить_некому` и
`test_с_потолком_вытесненный_назван` на одной и той же синтетической базе.

### Числа

    $ plan('...бюджет $0.05 за секунду')
      шаг липсинк: kling-video-lipsync-audio-to-video — применимость не измерена
          ПРОВЕРЕННЫЙ ЕСТЬ, НО ЦЕНА ЕГО ОТОДВИНУЛА: wav2lip —
              применимость измерена: 2 строк(и), цена: цена не записана.
              выбран kling-video-lipsync-audio-to-video, а не wav2lip:
              разошлись по «положение по цене» (в бюджете против цена не записана)

Негативные контроли на живой базе, оба:

    'нужен липсинк на готовое видео'  (без потолка)
        выбран wav2lip, прим=2, строка ПУСТАЯ
    'оживить фото... он говорит мою озвучку'
        озвучка   bark, прим=0 -> ПРОВЕРЕННЫХ НЕТ ВОВСЕ (из 9 найденных)
        оживление kling-3.0, прим=1 -> строка пустая
        липсинк   wav2lip, прим=2 -> строка пустая

Гейт: 11 брифов; вытеснен ценой на 1 шаге, проверенных нет вовсе на 4, строка
пропущена 0, лишняя 0. Тестов 61. Мутация Т1: 37 мутантов, промолчали на 0.
`bash scripts/check` EXIT=0.

### Убрана ложная ловушка в тесте

`test_контрольный_набор_читается_целиком` держал ТОЧНОЕ число брифов литералом
(8, потом 10). Он краснел на ЗДОРОВОМ дереве дважды подряд — просто оттого, что
в контрольный набор добавляли случай, — и оба раза таблица мутаций Т1
строилась поверх красного и не значила ничего. Литерал заменён ПОЛОМ (не
меньше восьми); равенство `rows_in() == len(briefs())`, ради которого тест и
писался, осталось. Тест обязан ловить ПОТЕРЮ контроля, а не его пополнение.

### Границы (И6)

* строка о вытесненном называет ОДНОГО кандидата, а не список. На брифе с
  потолком за секунду проверенных было четыре из 28; названный — сильнейший по
  доводам (`wav2lip`, 2 строки применимости). Остальные трое в выдачу
  по-прежнему не попадают, и это осознанная цена «одной строки на шаг»;
* «проверенных нет вовсе» на живой базе срабатывает часто — на 4 шагах из 13.
  Это не шум прибора, а состояние базы: применимость записана у меньшинства
  моделей, и теперь это видно на каждом таком шаге, а не выводится читателем.
