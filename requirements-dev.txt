# Runtime
numpy==2.4.6
pillow==12.3.0
# Tooling — pinned so local runs and CI run the same checks.
# Keep in sync with .pre-commit-config.yaml rev.
ruff==0.15.8
mypy==1.19.1
types-requests==2.33.0.20260712
# Studio web layer — pinned to what was installed and measured on 2026-08-25.
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32
# The chat agent (studio/mcp) — pinned to what was installed and measured on 2026-08-27.
mcp==2.1.1
