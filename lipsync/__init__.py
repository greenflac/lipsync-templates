"""lipsync — face photo + brief -> ball-jump video creative -> critic-ranked.

A walking-skeleton demo pipeline for the generative-video vacancy: integrate a
reference face, generate a short vertical clip of the person jumping on a
gymnastics ball, (optionally) lipsync in Russian, and let a critic cull the run.
Offline-reproducible on synthetic fixtures; live generation is a flag; the
image-to-video model is the declared, unbuilt gap.
"""

__version__ = "0.1.0"
