"""Lip-sync templates: a client selfie + a template -> a vertical video starring them.

Two tools: the generation pipeline (eight stages from selfie to video, seven
free checks before the one paid call) and the template builder (prompt +
demo person -> a sellable aesthetic). The product entry point is
`lipsync.fork_e2e`.
"""

__version__ = "0.1.0"
