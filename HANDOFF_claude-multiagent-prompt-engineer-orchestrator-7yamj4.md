# Ветка `claude/multiagent-prompt-engineer-orchestrator-7yamj4`

> **Этот файл был архивом на 2330 строк и ~39 000 токенов.** Каждая сессия
> читала их целиком, чтобы найти три числа, платя за это контекстом и дрейфом
> на каждом ходу. Владелец назвал это как есть 2026-08-31, и правило проекта
> говорило то же самое словами: факты о проекте в хэндоф не кладутся, у них
> свои адреса.
>
> Старое содержимое **никуда не делось** — оно в истории git, целиком, в
> коммите `2e1d995` и раньше. Ничего не потеряно, всё переехало по адресам.
>
> Теперь размер сторожит `scripts/check_measured.py` в общем гейте: потолок 400
> строк. Правило, которое обязано выполняться всегда, — это гейт, а не абзац
> (Ц7). Прежняя редакция правила была абзацем и не соблюдалась.
>
> `# DEBT(2026-08-31)`: правило Ц6 требует append-only, и этот файл переписан
> целиком, а не дописан. Основание: я единственный писатель этого хэндофа
> (append-only существует, чтобы параллельные агенты не затирали друг друга),
> а прежняя редакция нарушала более раннее правило того же документа. Отступление
> записано здесь, чтобы оно грепалось и не стало нормой.

## Куда что переехало

| Что | Адрес |
|---|---|
| числа про наши приборы, корпус, конвейер | `studio/knowledge/measured.jsonl` |
| отрицательные результаты с числами (И6) | там же, `outcome: не годно / не смогли` |
| что известно про чужие модели | `studio/knowledge/model_facts.jsonl` |
| заявки на платный замер | `studio/knowledge/measurements.jsonl` |
| откуда взят каждый корпус и на каком основании | `studio/knowledge/PROVENANCE.md` |
| почему конкретный код устроен так | докстрока рядом с этим кодом |
| что было сделано и почему | сообщение коммита |
| решения по RAG и план выхода | `docs/DECISIONS_RAG.md`, `docs/PLAN_RAG.md` |

Найти число, не читая ничего лишнего:

```bash
python scripts/check_measured.py --find civitai
python scripts/check_measured.py --find валидатор
```

## Что на ветке сделано

> С ЧЕГО НАЧАТЬ. Открыть «Открытые вопросы к владельцу» ниже: четыре из них
> ждут решения, и без них продукт двигают только правки формы, а не сути.
> Мерить сделанное: `python scripts/check_golden.py` (44 задачи, машинно все,
> регрессов 0, целей не взято 2) и `python scripts/check_spend_aim.py` (куда
> нацелены деньги). Полный гейт — `bash scripts/check`, около 6 минут.

**MCP-сервер, 15 инструментов.** Считать так:
`grep -c "^@server.tool()" studio/mcp/server.py` — с ЯКОРЕМ СТРОКИ. Число тут
разъезжалось трижды: стояло 14; я поправил на 15 по памяти; потом «починил» на
16 счётчиком БЕЗ якоря — а он ловит ещё и упоминание декоратора в докстроке на
строке 194. Верных 15, и это подтверждает `server.list_tools()`. Мораль не про
число: неверный прибор убедительнее памяти, потому что печатает цифру. Промты под конкретные модели, факты о моделях
с тиром и датой, поиск в вебе с первоисточником, разбор чужого креатива, заявки
на платный замер. Положение об универсальности первым абзацем в `instructions`
и засторожено тестом: агент не обязан предпочитать то, что лежит в этом репо.

**RAG на 130 записях ремесла плюс корпус промтов.** Две полки — «примеры» и
«знание» — выдаются раздельно; намеренный отказ строить роутер намерений.
Полка ЗНАНИЯ отвечает по-русски; полка ПРИМЕРОВ английская, и продукт говорит
это дословно. С 2026-09-04 русский запрос проходит через дверь
`studio/ruwords.py` и достаёт примеры (было 1 на шести намерениях, стало 30).

**Стенд-валидатор.** Свежий агент видит только картинку, истины у него нет
физически. Банк собирается из трёх источников с тремя грейдами истины, слепые
копии делает скрипт, а не руки.

**Голден-сет задач (2026-09-03).** 44 задачи в 24 классах, ось «сейчас/цель»
разделяет регресс и невзятую работу, негативный контроль ловит холостые
проверки. Оценщик — шаг гейта.

**Честность ответов (2026-09-03/04).** Уточняющий вопрос вместо догадки;
пометка дописанных шагов в печати; семьи атрибутов для поведения, входов,
выходов и предела текста (975 строк базы из 1569 стали достижимы вопросом);
свёртка версионных написаний имён; цена ВСЕГО пайплайна, никогда одним числом.

Все числа — в `measured.jsonl`.

## Что сторожит гейт

| Гейт | Что ловит |
|---|---|
| `scripts/check` | всё ниже, одной командой |
| `scripts/check_as_ci` | то же, но на HEAD в отдельном worktree — ровно взгляд CI |
| `scripts/validator.py` | происхождение не уезжает вместе с файлом к читателю |
| `scripts/check_sheet_shape.py` | форма файла не сортирует разборы по источнику |
| `scripts/check_craft.py` | у каждой записи знания есть источник и тир |
| `scripts/check_measured.py` | схема чисел и размер этого файла |
| `test_no_test_needs_the_uncommitted_corpus.py` | тест не зависит от того, чего нет в CI |

## Открытые вопросы к владельцу

> Сверху — то, что ждёт решения СЕЙЧАС. Ниже — давние, с их состоянием.
> Порядок не украшение: следующая сессия читает первый экран, и живой вопрос,
> лежащий под шестью закрытыми, читается как закрытый.

### Ждут решения (2026-09-03/04)

**В1. Одобрить четыре заявки на замер, $1.60 суммарно.** Поданы туда, куда
продукт смотрит на самом деле: `argil-avatars-text-to-video` и
`kling-video-o1-image-to-video` (holds_identity), `elevenlabs-tts-eleven-v3` и
`elevenlabs-dubbing-v2` (observed_behaviour). До них шесть заявок на $4.25
висели на моделях, которых планировщик НЕ ВЫБИРАЕТ НИ РАЗУ, — деньги смотрели
мимо продукта. Что это меняет: сегодня ни один план не получает исхода
«годно» (10 «не годно», 9 «не смогли», 0 «годно»), потому что применимость
почти нигде не измерена. Эти четыре — первое, что деньги сдвинут наблюдаемо;
у каждой в поле `decides` записано, какое решение она меняет. Сторожится
`scripts/check_spend_aim.py`.

**В2. Доступ к источникам скорости и отзывов практиков.** Две цели голден-сета
(g25 скорость, g26 наблюдения) не берутся не работой, а доступом:

* вендор публикует скорость РЕЙТИНГОМ (виджет `vertex-ai-latency-N` с
  ракетами), а не секундами — перевести ракеты в секунды значит выдумать;
* `ai.google.dev`, `docs.cloud.google.com` и `reddit.com` числятся открытыми,
  отвечают 200 и отдают пустую оболочку: тело собирается скриптом в браузере.
  Отчётов практиков через этот читатель не достать.

Нужно от вас: либо источник, который читается (например, доступ к чему-то
кроме HTML-оболочек), либо решение, что скорость и отзывы мы не обещаем.
Обходить закрытое не буду (Ц3).

**В3. Ставить ли `insightface` в окружение.** Без него половина входа «готовый
креатив» недоступна: кадр, палитра и свет меряются, лицо — нет. Продукт об
этом говорит честно («nothing to ask with… This is not there is no face»), но
работы не делает. Пакет числится «давним необъявленным» вместе с `mediapipe`
и `fal-client`; `lipsync/**` заморожен, и я туда не лезу (Ц2).

**В4. Оговорка о метрике считается отрицательной применимостью — так и надо?**
ИЗМЕРЕНО абляцией 2026-09-04, и число резкое: ВСЕ 10 отказов плана в
голден-сете дают ОДИН класс, ОДНА модель и ОДНА строка.

    как есть                        10 «не годно», 9 «не смогли»
    без latentsync.metric_blind_spot 1 «не годно», 18 «не смогли»
    без latentsync.failure_mode     без изменений — эта строка стоит НОЛЬ

Спорная строка говорит: «двухэтапные архитектуры теряют эмоциональный нюанс, и
SyncNet этого не видит». Это оговорка о МЕТРИКЕ (оценка может быть завышена),
а не наблюдение, что модель ломается по требованию шага. Сейчас валидатор
читает её как «применимость отрицательная» и выносит продукту самый суровый
приговор на девяти планах из десяти.

«Годно» не появляется ни в одном варианте — применимость всё равно не
измерена. Разница ровно в том, что продукт говорит человеку: «плохо» или «не
проверили». Классификацию я НЕ трогал: она живёт в `studio/factaxis.py`, за её
решениями стоят разобранные случаи, и три моих подозрения к этому коду за
сессию не подтвердились. Решение — ваше.

### Давние

1. ~~Приватный репозиторий под корпус~~ — **СДЕЛАНО 2026-08-31.**
   `greenflac/lipsync-corpus`, приватность проверена командой до заливки
   (`private: true`), первая связка 17.6 МБ залита, сверка с манифестом
   `exit 0`. Обновляется одной командой:

   ```bash
   python scripts/corpus_bundle.py --out /home/user/lipsync-corpus
   python scripts/corpus_bundle.py --verify /home/user/lipsync-corpus
   git -C /home/user/lipsync-corpus commit -am "..." && git -C ... push
   ```

   Медиа банка (253 МБ) туда НЕ едет намеренно — рецепта достаточно, и чужие
   ролики мы не перепубликуем. Ограничение записано в README репозитория.
2. **Серверный лог второго видео-вендора.** Всё не-Kling видео сейчас помечено
   загрузчиком, и различить «агент слеп» от «метка шумит» нечем.
3. **Баланс Kling.** Заявка `mp-eaed2081b8` одобрена, вызов вернул
   «недостаточно средств», потрачено $0.
4. **Плотный поиск — лицензионного блокера БОЛЬШЕ НЕТ.** Проверено 2026-08-31
   командой: библиотека `Apache-2.0` (PyPI, `license_expression`), модель
   `all-MiniLM-L6-v2` тоже `apache-2.0` (HF `cardData` и тег). Осталось
   инженерное решение — включать ли канал и ставить ли пакет в requirements.
5. **Ступенька лестницы для собственного опыта оператора.** Ваша поправка про
   nano-banana была именно таким знанием, и системе его некуда положить.
6. **Домены `archive.civitai.com` и `civitarchive.com`** закрыты шлюзом (502 и
   403). Не обходил (Ц3). Если источник нужен — назвать владельцу окружения.

## Правила, которые тут уже нарушались

- Числа и разборы багов — не сюда. Для них есть адреса выше.
- Тест, которому нужен корпус, зелен здесь и красен в CI. Ловится AST-тестом.
- Гейт и коммит одной командой прячут код возврата: `echo "GATE EXIT=$?"`
  отдельным вызовом.
- Реформат кода однажды тихо откатил правку фильтра ffmpeg. После форматирования
  результат смотрится глазами (П3), а не принимается на веру.

## Куда уехала разобранная работа

Разделы с уже закоммиченной работой перенесены в
`HANDOFF_ARCHIVE_claude-multiagent-prompt-engineer-orchestrator-7yamj4.md`.
Здесь остаётся состояние ветки, открытые вопросы и самая свежая находка.


Перенесено 2026-09-04: четыре раздела ратчета R7 (69 → 65 → 62 → 59 → 56),
у каждого есть коммит. Живой хэндоф упёрся в потолок 400 строк, и гейт это
поймал на пуше — потолок работает.

Перенесено 2026-09-05 вторым заходом: разбор первого ложного «годно» за $0.08 и
раздел про две утечки мутационного прогона — 139 строк, у всего есть коммиты.

Перенесено 2026-09-05: решения владельца по шести вопросам, раздел про пул
применимости, третий случай того же класса в ратчете и отчёт об исполнении
трёх оплаченных заявок ($0.92) — 154 строки, у всего есть коммиты. Потолок
поймал снова, на том же пуше, тем же способом.

## Куда уехали разборы 2026-09-05

Два раздела — «Вечер 2026-09-05: R3 закрыт, две независимые проверки, восемь
находок» и «Ночь 2026-09-05: третья и четвёртая независимые проверки, 14
находок» — перенесены ЦЕЛИКОМ в архив ветки: у них есть коммиты, значит это
уже история, а не состояние. Живой хэндоф держится под потолком 400 строк
(`scripts/check_measured.py`), иначе его перестают читать с начала.

## Куда уехали восемь находок четвёртой проверки

Разделы про находки №1–№8 (все закрыты коммитами 905929f, c475926, ee14166,
9001953, af7b658) перенесены целиком в архив ветки: у них есть коммиты, значит
это история. Итог одной строкой: охрана возвращена и усилена, долг R7 не
изменился (37) — та серия чинила ОХРАНУ, а не покрытие.

## 2026-09-06 — НАБЛЮДЕНИЕ О ПАРАЛЛЕЛЬНОЙ ПРОВЕРКЕ: она чистит рабочее дерево

Независимая проверка получает задание «всё откатывай, `git status` в конце
чистый» — и откатывает ВСЁ ДЕРЕВО, включая мою НЕЗАКОММИЧЕННУЮ работу. Сегодня
так пропали правка mypy и перенос разделов хэндофа в архив; обнаружилось по
тому, что гейт второй раз упал на уже исправленной ошибке.

ПРАВИЛО ОТСЮДА: не держать незакоммиченного, пока идёт параллельная проверка.
Коммит — единственное место, куда она не дотянется откатом.

## 2026-09-06 — ПЯТАЯ независимая проверка: 32 утверждения подтверждены, 4 своих дефекта

Проверка воспроизвела ВСЕ 22 подмены пяти коммитов (не половину, как просили) и
все 10 измерений: подтвердилось 32 из 32, опровергнуто 0, ERROR=0 везде — то
есть тесты краснеют по assert'у, а не по исключению. Снятых молча охран в этих
коммитах не нашла: тестов стало 9→15, 10→12, 7→8, 8→9, 6→9, 8→12, 13→20, 8→9;
два удалённых теста заменены более строгими.

ЕЁ ЧЕТЫРЕ НАХОДКИ, ВСЕ ЗАКРЫТЫ:

1. **Е1 держался с одной стороны.** Тождество `registrable` проверял только
   тест заявки-whitelist; копию можно было молча вернуть в `allowlist_request.py`
   — ровно то, из-за чего коммит и написан. Тождество теперь проверяется с обеих
   сторон, подмена «копия вернулась» — FAILED(2).
2. **`unmeasured` литеральным нулём в приборе R7** — том самом, который накануне
   чинили ради Р2. `SyntaxError` глушился через `continue`: половина дерева могла
   не разобраться, а прибор напечатал бы «проверено 54, нарушений 16, не смогли 0»,
   и ратчет потребовал бы опустить потолок — решение по несостоявшемуся измерению.
   Теперь неразобранные считаются, печатаются поимённо и не дают «годно».
3. **Текст отказа обещал PDF**, переставший быть признаком в том же коммите:
   `server.py` поправили, а ноту отказа — нет. Пишущий читает ноту.
4. **DOI искался и в СТРОКЕ ЗАПРОСА**: `blog.example.com/x?ref=10.1145/…` проходил
   как статья. Та же форма, что и `openreview.net` подстрокой, — значит И7.
   Разбор теперь смотрит только путь, а идентификатор arxiv засчитывается лишь на
   площадке или её зеркале (`vendor.example/pricing/2401.12345/` — не статья).

Плюс измеренная граница (И6): из 15 настоящих адресов статей отвергались 7.
Добавлены AAAI, USENIX, CVF-ECVA, Semantic Scholar, JSTOR, NIH; проб 12 —
расхождений 0. Живая база не сдвинулась: paper 184, признак несут 183.

Мутантов 410 → 414. Ran 1625 tests, OK. Полный гейт на предыдущем коммите —
зелёный, 579 с.

## Ратчет R7: 37 -> 36 (2026-09-06)

Опущен не «за работу», а потому что прибор R7 встал под собственных мутантов,
пока чинились его же нарушения Е2 и Р2. Модулей с константами-решениями 111,
под мутантами 75, без мутантов 36. Полный гейт сам этого и потребовал:
«ПОТОЛОК УСТАРЕЛ ... опустите до 36, иначе он перестанет ловить рост».

## Ратчет R7: 36 -> 35 (2026-09-06), `studio/measured.py`

Адрес измеренных чисел: `ORIGINS` и `OUTCOMES` — это правила И4 и Р1,
записанные ДАННЫМИ, а значит правило можно отменить, дописав строку в кортеж.
Подменой измерено, что так и было: четвёртое происхождение «ПРИКИНУТО»
дописывалось МОЛЧА (все тесты зелёные), и цикл проверки обязательных полей
выключался целиком тоже молча. Теперь обе таблицы литералами и каждое
обязательное поле проверяется поодиночке; подмены — FAILED(1), FAILED(1),
FAILED(2), FAILED(2), ERROR=0 везде.

Модулей 111, под мутантами 76, без мутантов 35. Мутантов 414 -> 416.

## Ратчет R7: 35 -> 34 (2026-09-06), `studio/corpus.py`

Наборы `APPLICABILITY` и `WITNESSED_TIERS` охранялись сторожами сужения ровно
на два имени. Подменой измерено: вынуть `observed_behaviour` — самый частый
атрибут канала (6 наблюдений из 11 на живом замере) — можно было МОЛЧА, и
покрытие применимости завысилось бы; дописать `blog` в свидетели — тоже молча,
а это заголовочное число «кто-то это ЗАПУСКАЛ». Оба набора теперь литералами,
плюс негативный контроль «блог свидетельством не становится».

Подмены: применимость сузилась — FAILED(2); блог в свидетелях — FAILED(2);
применимость расползлась — FAILED(2); откат OK. Мутантов 416 -> 418.
Модулей 111, под мутантами 77, без мутантов 34.

## Ратчет R7: 34 -> 33 (2026-09-06), `studio/mcp/civitai.py`

ЧЕСТНО ПРО ЭТОТ ШАГ: новой охраны не появилось. Обе константы-решения канала
(`MAX_NSFW_LEVEL = 4`, `MIN_PROMPT_WORDS = 3`) УЖЕ краснели в обе стороны, но
ни один зарегистрированный мутант на них не указывал — то есть ратчет считал
модуль голым, а он им не был. Проверено подменами до регистрации: потолок снят
— FAILED(4), потолок до нуля — FAILED(4), минимум слов снят — FAILED(3) (модуль
`test_civitai`, а не `test_civitai_facts` — второй эту развилку не видит).

Записано отдельно, чтобы число R7 не читалось как «стало безопаснее»: здесь
стало ТОЧНЕЕ. Мутантов 418 -> 420, без мутантов 33.

## Ратчет R7: 33 -> 32 (2026-09-06), `scripts/retier_facts.py`

`decide` — сердце перетирания базы, и его не касался НИ ОДИН тест. Модуль
обещает в шапке «повторный прогон безопасен, это чистая функция» — держится
обещание ровно этой функцией. Подменой измерено, что охраны не было:
`METHOD_TIERS = ()` (ступени-методы начинают перезаписываться по адресу) и
метка пересказа, переставшая опознаваться, — оба прогона ЗЕЛЁНЫЕ.

Семь тестов на все пять развилок, ожидаемое литералами, диска и сети нет.
Подмены после: ступени-методы перезаписываются — FAILED(1); метка пересказа —
FAILED(1); закрытый хост не влияет — FAILED(1); неизвестное объявляется
непрочитанным (тот самый дефект на 1007 строк) — FAILED(1); зонд не считается
прочитанным — FAILED(1); откат OK. Мутантов 420 -> 422, без мутантов 32.

## Ратчет R7: 32 -> 31 (2026-09-06), `scripts/validator.py`

Два теста бенча проверяли не то, что называли:

`test_the_same_seed_draws_the_same_cases` сравнивал ДВА ВЫЗОВА В ОДНОМ ПРОГОНЕ
— одинаковый результат он даёт при любом зерне; подмена `SEED = 1` оставалась
зелёной. Воспроизводимость бенча стоит на конкретном числе: с другим зерном
заявленный признак перепроверяется на другой выборке, и сравнение ничего не
значит. Теперь зерно литералом и отобранные шесть разборов названы по id.

`test_every_configuration_is_represented` брал `bench.PER_CONFIG` — ожидаемое
ехало вместе с модулем (Т2), и подмена «по одному разбору на конфигурацию»
молчала. Это вдвое меньший бенч при том же отчёте.

Отдельно поймано: фикстура без адресов картинок давала ВСЕМ разборам один id
(`case_id` считается по адресу), то есть выборка выглядела одинаковой при любом
зерне. Фикстура заменена на адресованную.

Подмены: зерно уплыло — FAILED(1); по одному на конфигурацию — FAILED(2); в
info разрешено всё подряд — FAILED(5); откат OK. Мутантов 422 -> 424,
без мутантов 31.

## Ратчет R7: 31 -> 29 (2026-09-06), верность и чужая проза

`studio/selfrag/fidelity.py` — НАСТОЯЩАЯ дыра. `FORMAT_WORDS` — это то, что
проверка ВЫЧИТАЕТ перед решением, значит каждое дописанное туда слово это одна
выдумка, которую она больше не видит. Подменой измерено: дописать `teal`,
`vermilion`, `portrait` можно было МОЛЧА. Теперь цвет в «палитре из…» обязан
опознаваться выдумкой, а два законных слова строительных лесов (`palette`,
`mood`) держит негативный контроль — их доклад и был дефектом 2026-08-26.

Своя же фикстура поймала свою ошибку: первым вариантом я взял `amber`, а он
СТОИТ в исходном запросе и потому выдумкой не является. Тест покраснел на мне
раньше, чем я успел его закоммитить.

`studio/verbatim.py` — учёт, а не новая охрана: все четыре константы-решения уже
краснели в обе стороны, мутанта на них не было.

Мутантов 424 -> 427, модулей 111, под мутантами 82, без мутантов 29.
