# Ветка `claude/multiagent-prompt-engineer-orchestrator-7yamj4`

> **Этот файл был архивом на 2330 строк и ~39 000 токенов.** Каждая сессия
> читала их целиком, чтобы найти три числа, платя за это контекстом и дрейфом
> на каждом ходу. Владелец назвал это как есть 2026-08-31, и правило проекта
> говорило то же самое словами: факты о проекте в хэндоф не кладутся, у них
> свои адреса.
>
> Старое содержимое **никуда не делось** — оно в истории git, целиком, в
> коммите `2e1d995` и раньше. Ничего не потеряно, всё переехало по адресам.
>
> Теперь размер сторожит `scripts/check_measured.py` в общем гейте: потолок 400
> строк. Правило, которое обязано выполняться всегда, — это гейт, а не абзац
> (Ц7). Прежняя редакция правила была абзацем и не соблюдалась.
>
> `# DEBT(2026-08-31)`: правило Ц6 требует append-only, и этот файл переписан
> целиком, а не дописан. Основание: я единственный писатель этого хэндофа
> (append-only существует, чтобы параллельные агенты не затирали друг друга),
> а прежняя редакция нарушала более раннее правило того же документа. Отступление
> записано здесь, чтобы оно грепалось и не стало нормой.

## Куда что переехало

| Что | Адрес |
|---|---|
| числа про наши приборы, корпус, конвейер | `studio/knowledge/measured.jsonl` |
| отрицательные результаты с числами (И6) | там же, `outcome: не годно / не смогли` |
| что известно про чужие модели | `studio/knowledge/model_facts.jsonl` |
| заявки на платный замер | `studio/knowledge/measurements.jsonl` |
| откуда взят каждый корпус и на каком основании | `studio/knowledge/PROVENANCE.md` |
| почему конкретный код устроен так | докстрока рядом с этим кодом |
| что было сделано и почему | сообщение коммита |
| решения по RAG и план выхода | `docs/DECISIONS_RAG.md`, `docs/PLAN_RAG.md` |

Найти число, не читая ничего лишнего:

```bash
python scripts/check_measured.py --find civitai
python scripts/check_measured.py --find валидатор
```

## Что на ветке сделано

**MCP-сервер, 14 инструментов.** Промты под конкретные модели, факты о моделях
с тиром и датой, поиск в вебе с первоисточником, разбор чужого креатива, заявки
на платный замер. Положение об универсальности первым абзацем в `instructions`
и засторожено тестом: агент не обязан предпочитать то, что лежит в этом репо.

**RAG на 130 записях ремесла плюс корпус промтов.** Две полки — «примеры» и
«знание» — выдаются раздельно; намеренный отказ строить роутер намерений.
Поиск работает по-русски и по-английски.

**Стенд-валидатор.** Свежий агент видит только картинку, истины у него нет
физически. Банк собирается из трёх источников с тремя грейдами истины, слепые
копии делает скрипт, а не руки.

Все числа — в `measured.jsonl`.

## Что сторожит гейт

| Гейт | Что ловит |
|---|---|
| `scripts/check` | всё ниже, одной командой |
| `scripts/check_as_ci` | то же, но на HEAD в отдельном worktree — ровно взгляд CI |
| `scripts/validator.py` | происхождение не уезжает вместе с файлом к читателю |
| `scripts/check_sheet_shape.py` | форма файла не сортирует разборы по источнику |
| `scripts/check_craft.py` | у каждой записи знания есть источник и тир |
| `scripts/check_measured.py` | схема чисел и размер этого файла |
| `test_no_test_needs_the_uncommitted_corpus.py` | тест не зависит от того, чего нет в CI |

## Открытые вопросы к владельцу

1. ~~Приватный репозиторий под корпус~~ — **СДЕЛАНО 2026-08-31.**
   `greenflac/lipsync-corpus`, приватность проверена командой до заливки
   (`private: true`), первая связка 17.6 МБ залита, сверка с манифестом
   `exit 0`. Обновляется одной командой:

   ```bash
   python scripts/corpus_bundle.py --out /home/user/lipsync-corpus
   python scripts/corpus_bundle.py --verify /home/user/lipsync-corpus
   git -C /home/user/lipsync-corpus commit -am "..." && git -C ... push
   ```

   Медиа банка (253 МБ) туда НЕ едет намеренно — рецепта достаточно, и чужие
   ролики мы не перепубликуем. Ограничение записано в README репозитория.
2. **Серверный лог второго видео-вендора.** Всё не-Kling видео сейчас помечено
   загрузчиком, и различить «агент слеп» от «метка шумит» нечем.
3. **Баланс Kling.** Заявка `mp-eaed2081b8` одобрена, вызов вернул
   «недостаточно средств», потрачено $0.
4. **Плотный поиск — лицензионного блокера БОЛЬШЕ НЕТ.** Проверено 2026-08-31
   командой: библиотека `Apache-2.0` (PyPI, `license_expression`), модель
   `all-MiniLM-L6-v2` тоже `apache-2.0` (HF `cardData` и тег). Осталось
   инженерное решение — включать ли канал и ставить ли пакет в requirements.
5. **Ступенька лестницы для собственного опыта оператора.** Ваша поправка про
   nano-banana была именно таким знанием, и системе его некуда положить.
6. **Домены `archive.civitai.com` и `civitarchive.com`** закрыты шлюзом (502 и
   403). Не обходил (Ц3). Если источник нужен — назвать владельцу окружения.

## Правила, которые тут уже нарушались

- Числа и разборы багов — не сюда. Для них есть адреса выше.
- Тест, которому нужен корпус, зелен здесь и красен в CI. Ловится AST-тестом.
- Гейт и коммит одной командой прячут код возврата: `echo "GATE EXIT=$?"`
  отдельным вызовом.
- Реформат кода однажды тихо откатил правку фильтра ffmpeg. После форматирования
  результат смотрится глазами (П3), а не принимается на веру.

## Куда уехала разобранная работа

Разделы с уже закоммиченной работой перенесены в
`HANDOFF_ARCHIVE_claude-multiagent-prompt-engineer-orchestrator-7yamj4.md`.
Здесь остаётся состояние ветки, открытые вопросы и самая свежая находка.

## Два отрицательных результата по каналам мониторинга (2026-09-02)

Записаны затем, чтобы следующая сессия не мерила это заново (И6).

### Карточки моделей (README) как источник цены и скорости — не окупаются

ИЗМЕРЕНО на восьми крупных карточках (LTX-Video, Wan2.2-I2V-A14B,
HunyuanVideo, CogVideoX-5b, InfiniteTalk, LatentSync-1.6, mochi-1-preview,
Allegro): строк о скорости, которые ловятся построчно, — НОЛЬ; строк о
минимальной памяти — ТРИ на восемь карточек; таблиц со временем прогона — две.

Причина нулей не в приборе: вендоры кладут скорость в ТАБЛИЦЫ («Total time (s)
/ peak GPU memory (GB)» у Wan), а таблица — это числа в ячейках, разбросанных
по строкам. Разбирать их — значит сопоставлять колонки, и цена ошибки высокая:
ровно сегодня канал уже сочинил «GTX1070 39 GB», взяв число не оттуда.

Вывод: карточку читать ради лицензии и принятости (это работает), а таблицы
скорости не разбирать, пока не появится способ проверить сопоставление колонок.
Одиночные фразы вида «at least 80GB VRAM» разбирать можно — их три на восемь
карточек, то есть отдельного канала они не окупают.

### GitHub issues недоступны этому агенту

`api.github.com` объявлен открытым хабом и на деле отвечает **403 Forbidden**
на `/repos/{owner}/{repo}/issues` без токена — проверено на
`kijai/ComfyUI-WanVideoWrapper` и `Lightricks/LTX-Video`. Инструменты GitHub в
этой сессии ограничены одним репозиторием (`greenflac/lipsync-templates`), так
что читать чужие issues нечем.

Это ЖАЛЬ: база уже цитирует issues ComfyUI-WanVideoWrapper — их кто-то читал
руками, — и практики отчитываются там о прогонах подробнее, чем на HuggingFace.
Канал не строится не потому, что бесполезен, а потому что доступа нет. Нужен
токен или решение владельца; обходить (Ц3) нельзя.

## 2026-09-02, продолжение: цена, планировщик, заход по 54 моделям

**Разворот имени атрибута** (`studio/selfrag/attrfamily.py`, коммит b0bc4a1).
ИЗМЕРЕНО: цена записана у 79 моделей, на вопрос `price` отвечали 7 — она лежит
под 18 именами. После правки отвечают 75 из 75; «скорость» — 10 моделей вместо
5. Негативный контроль семьи закрытый и обязателен: `fps`, `speed_range`,
`rendering_speed`, `training_time`, `indexing_latency`, `price_relative` —
звучат как ответ, означают другое. 6 мутантов, промолчало 0.

**Планировщик пайплайна** слит из `studio/pipeline-planner` (a9e77b2), числа
автора воспроизведены независимо (И1). Чтением выдачи найдено: названный
словами бюджет («бюджет 0.5 доллара») не разбирается и не влияет на отбор —
выбран `wav2lip` с незаписанной ценой при `latentsync` строкой ниже, у
которого цена в базе есть и в бюджет укладывается. Возвращено автору модуля.

**Заход HF по 54 моделям** (40c8ff0): 11 новых строк, 8 из 19 — вытеснение уже
записанного. Две находки чтением глазами: ложная поломка («I failed to find
what advantage...» — человек не понял, модель цела) и потеря имени второго
файла лицензии при вытеснении.

**НЕ ДЕЛАТЬ (оплачено сегодня).** Дедупликация по живой базе. База append-only,
повтор в ней — история (вытеснение, отзыв, перезапись), а не мусор; прогон
дедупликации снёс 200+ законных строк, восстановлено из HEAD.

**ПРОВЕРЕНО И НЕВЕРНО (И6).** «Две одинаковые строки читаются как два
источника» — нет: `advice.record` вытесняет по ключу
(модель, атрибут, значение, URL), база отвечает «from 1 source(s)».

**Открыто для владельца:** ники остаются в истории git (снятие = переписывание
истории и force-push при открытом PR); канал GitHub issues требует токена либо
закрывается; 25.75 $ из 30 не расписаны, пока не прочитаны страницы цен.

## 2026-09-02, вторая половина: цена, вендорские страницы, гейт на заголовок

**Канал портала** (`scripts/ingest_portal.py`, a202981). 28 строк с fal.ai, из
них 22 `price_per_second_usd`. Лицензия портала НЕ пишется у моделей: 66 из 66
карточек `commercial` — это условия перепродажи, не лицензия весов, и у модели
под «research only» дала бы ложный зелёный на Ц5. Ошибка в сорок раз, пойманная
на своей выдаче: единица засчитывается только рядом с ПЕРВОЙ суммой, иначе
«$0.2 за ролик до 40 секунд, дальше $0.005 за секунду» читалось как $0.2/сек.

**Мониторинг вендорских страниц** (`scripts/recheck_vendor.py`, d5ffae0). 32
хоста открыты, 71 адрес, 69 под наблюдением. Второй прогон дал 12 ложных
тревог — флаги эксперимента внутри `<script>`; после чистки скриптов «как
прежде 69 из 71». СПОСОБ отпечатка едет в журнал: отпечатки разных правил
сравнивать нельзя. Гейтом НЕ ставится — ходит в сеть (Т4).

**Гейт на верхнюю строку** (`scripts/check_headline.py`, 51effb6). Семь
дефектов дня были одного класса: честно в деталях, неверно в заголовке.
Проверено мутацией: выключенный разворот имён даёт 94 расхождения, включённый —
0. Полный обход по моделям со строкой семьи, выборка 40 по остальным.

**Асимметрия знаков** (56b6046). Провал ловился обычными словами, удача —
идиомами: 221 против 6. Наивное расширение дало бы 21 строку из 83 с четырьмя
ложными; с правилами «оценка рядом с исходом» и «похвала с оговоркой не удача»
— 5, все верные. Старые строки НЕ переписываются: смена атрибута делает новое
утверждение, а не поправку.

**НЕ ДЕЛАТЬ (оплачено сегодня).**
* Дедупликация по живой базе — снесла 200+ законных строк (append-only).
* Перезапись очереди портала неполным опросом — красит CI; теперь запрещено кодом.
* Повторный прогон канала ради переклейки атрибута — плодит вторую строку.

**ПРОВЕРЕНО И НЕВЕРНО (И6).**
* «Две одинаковые строки читаются как два источника» — нет, вытеснение по ключу.
* «Знаки удачи и провала сталкиваются и глушат друг друга» — нет, все 83
  безнаковые строки не содержат ни одного знака.

**ИЗМЕРЕННАЯ ГРАНИЦА.** Сообщество пишет о поломках примерно в пять раз чаще,
чем об удачах. Канал это отражает, а не выдумывает баланс.

## 2026-09-02, третья часть: семьи атрибутов, кадр, жизненный цикл

**Пять семей атрибутов** (`studio/selfrag/attrfamily.py`). Один и тот же промах
ключа найден на пяти вопросах; числа сняты ЧЕРЕЗ `advise`, не по сырым строкам:

    цена         79 моделей, отвечали  7 -> 75
    лицензия    253 моделей, отвечали 238 -> 252 (11 из 14 молчавших имели
                НЕКОММЕРЧЕСКУЮ оговорку — ошибка в опасную сторону)
    длительность 45 моделей, отвечали 25 -> 42
    разрешение   40 моделей, отвечали  2 -> 39
    скорость      —            отвечали  5 -> 10

Негативные контроли закрытые и выведены из ЗНАЧЕНИЙ: `fps`, `speed_range`,
`training_time`, `price_relative`, `portal_license`, `max_audio_seconds`,
`max_input_seconds`, `max_frames`, `training_resolution`.

**Гейт на заголовок** (`scripts/check_headline.py`): 2140 вопросов по пяти
семьям, расхождений 0. Проверен мутацией того самого дефекта: выключенный
разворот даёт 94 расхождения.

**Кэш базы фактов** по ключу (путь, mtime_ns, размер): 43 с -> 3.3 с на 1182
вопросах. Свежесть — следствие ключа, а не обещание.

**Кадр креатива** (`studio/resolution.py`, `scripts/creative_fit.py`) и его
приход в планировщик. `720p` — высота при ЛЮБОЙ ширине, вторая сторона НЕ
достраивается по 16:9. «up to 4MP» не разбирается: площадь без соотношения
сторон вопроса не решает.

**Жизненный цикл** (`studio/lifecycle.py`): 6 строк у 5 моделей, у двух срок
ПРОШЁЛ (`imagen-4`, `imagen-4.0`). Снятие называется в ноте, не только полем.

**НЕ ДЕЛАТЬ (оплачено сегодня).**
* Мутировать константу, которая стала производной: мутант бьёт по копии и
  молчит. Проверять надо там, где значение ЖИВЁТ.
* Оставлять старую функцию после рефакторинга: мёртвая копия правил ловит на
  себя мутанта (`про_снятие` vs `все_места_снятия`).
* Считать дефекты по сырым строкам вместо `advise`: цифра мимо продукта
  называет работой то, чего нет (15 против 14 на лицензиях).
* Брать дату/число из строки без проверки, ЧЕМУ они принадлежат: обучающая
  отсечка как срок снятия, длительность ролика как цена за секунду.

**ПРОВЕРЕНО И НЕВЕРНО (И6).** «`advise` и сырые строки дают одно и то же» — нет,
`advise` сводит написания. «Хватит близости числа к слову» — нет, у `gpt-5`
отсечка стоит в 60 знаках от `Deprecated`.

## 2026-09-02, итог захода: что стоит на ветке и что открыто

**Корпус:** 1968 утверждений, 543 модели. Применимость — 241 строка у 50
моделей, и это ПОТОЛОК СБОРА: 225 из 241 пришли из статей и бенчмарков, 16 —
от тех, кто действительно запускал. Собором эту цифру не поднять (проверено
дважды, см. ниже).

**Каналы:** HuggingFace (обсуждения), Civitai, портал fal.ai (цены, описания,
схемы входа и выхода), наблюдение за вендорскими страницами по отпечатку.

**Ось «воспользоваться нельзя»** в планировщике: кадр креатива, запрет на вход,
конец службы, вид выхода — ОДНО поле ключа, четыре причины, три исхода у
каждой. «Не записано» нигде не равно «можно».

**ДВАЖДЫ ИЗМЕРЕННЫЙ ОТРИЦАТЕЛЬНЫЙ РЕЗУЛЬТАТ (И6).** Связка «шагу нужен вход X →
модель принимает X» пока бесполезна: из 15 показанных кандидатов на трёх
брифах вход записан у ОДНОГО. Добавление `positioning` подняло число
кандидатов (17→19, 30→33), но перекрытие не изменило. Сбор портальных
метаданных дошёл до предела полезности; дальше двигает только применимость,
то есть прогоны.

**НЕ ДЕЛАТЬ (оплачено сегодня, полный список).**
* Дедупликация по живой базе — append-only, повтор это история.
* Перезапись очереди портала неполным опросом.
* Повторный прогон канала ради переклейки атрибута — плодит вторую строку.
* Мутировать константу, ставшую производной, — мутант бьёт по копии и молчит.
* Оставлять старую функцию после рефакторинга — мёртвая копия ловит мутанта.
* Считать дефекты по сырым строкам вместо `advise` — цифра мимо продукта.
* Брать число из строки, не проверив, ЧЕМУ оно принадлежит (обучающая отсечка
  как срок снятия, длина ролика как цена за секунду, длительность вместо цены).
* Проверять пересказ подстрокой по странице — значения в базе НЕ цитаты.

**ОТКРЫТО ДЛЯ ВЛАДЕЛЬЦА.**
1. Ники остаются в истории git; снятие = переписывание истории и force-push
   при открытом PR.
2. Канал GitHub issues требует токена либо закрывается (403 без него).
3. 25.75 $ из 30 не расписаны: цены прогонов не прочитаны.
4. НОВЫЙ ВОПРОС: перечитанный и неизменившийся источник не сбрасывает часы
   протухания — `stated_on` обязан оставаться датой источника. Сейчас очередь
   показывает наблюдение рядом с возрастом; надо ли, чтобы оно влияло на
   порядок, — решение продуктовое.

## 2026-09-03, заход: гейт не запускал 127 собственных тестов

**Дефект.** `scripts/check` называл модули `studio/tests` поимённо. Семь
файлов не назывался никто: `test_app`, `test_jobs`, `test_ledger`,
`test_lifecycle`, `test_store`, `test_style`, `test_vector_cache` — 127
тестов, среди них тесты модуля, написанного в тот же день. Все они проходили
при ручном запуске и не гонялись гейтом ни разу. Т6: «не запускалось»
выглядело как «прошло».

**Почему каталог был закрыт — две причины, ни одна не была названа вслух.**
Не было `studio/tests/__init__.py` (перебор падал ImportError), и `httpx2`
приезжал транзитивно через `mcp`, то есть набор зависел от чужого пина.

**НЕ ДЕЛАТЬ (оплачено сегодня).** Проверять «пакет нужен?» на своей машине.
Первый заход показал 503 зелёных теста и я почти записал вывод — а пакет
стоял локально как транзитивная зависимость `huggingface_hub`. Проверено в
ЧИСТОМ venv из одного `requirements-dev.txt`: снял `httpx2` — `test_app`
падает и сам называет пакет; вернул — 503 зелены. Первый негативный контроль
я к тому же поставил на `httpx` вместо `httpx2` и получил «пин ничего не
меняет» — контроль, снявший не тот пакет, читается как «фикс не нужен».

**Что стоит теперь.**
* `scripts/check_tests_gated.py` (Ц7): перечисляет файлы тестов в дереве и
  требует достижимости — перебором каталога или поимённым списком. Читает
  сам гейт, а не копию списка (Е1). Три исхода: разорванная цепочка
  `__init__.py` — «не смогли», имя мимо образца `test*.py` при целой цепочке
  — «не годно». Негативный контроль печатается первой строкой. Проверено
  воспроизведением: на СТАРОМ тексте гейта называет ровно те семь файлов.
* `scripts/check_skips.py` брал список наборов литералом и про `studio/tests`
  не знал — 503 теста и два пропуска в них не сторожил никто. Список теперь
  выводится из гейта: сторож вырос с 2330 до 2833 тестов.
* Гейт: 91 с -> 106 с, шесть поимённых шагов убраны как дубли перебора.
* Мутации каналов 24 -> 26, промолчали на 0.

**ПРОВЕРЕНО И НЕВЕРНО (И6).** Две мутации промолчали при первом заходе, и обе
оказались настоящими дырами, а не ложной тревогой: заслон по имени файла был
недостижим (обход искал только `test_*.py`, то есть образец `test*.py` не мог
не совпасть — расширил обход до `*_test.py`/`tests_*.py`), и причина пропуска
«фикстур нет» не сторожилась ничем.

**Открытые вопросы владельцу — те же четыре, ответов нет.**

## 2026-09-03, заход второй: вход «с нуля» не работал

**Дефект.** Планировщик выводит шаги по подсказкам-подстрокам из брифа. Тот, у
кого есть готовый креатив, говорит операциями («переозвучить», «попасть в
губы») — и это работало. Тот, у кого нет ничего, называет РЕЗУЛЬТАТ («нужен
говорящий аватар») — и получал ПУСТОЙ план: 8 брифов из 10.

**Счётчик раньше ручки (П1).** `scripts/check_brief_cues.py` +
`studio/knowledge/brief_wordings.jsonl` (28 брифов: сказанные операциями,
сказанные результатом, десять чужих задач для контроля).

**Набор окупился до первой правки.** Расширенный контроль нашёл ДВА места, где
продукт лгал: «фотосессия продукта с живым фотографом» -> `оживление` (по
подсказке `фото`), «права на голос актёра» -> `озвучка` (по `голос`). Подстрока
не отличает заказ работы от разговора о ней. Заведено `Operation.кроме`.

**Числа.** Сказано результатом: непустой план 2->8 из 10, все ожидаемые
операции 0->3 из 10. Сказано операциями: 8 из 8 без изменений. Чужие задачи:
ложных шагов 2 -> 0.

**НЕ ДЕЛАТЬ (оплачено сегодня).**
* Подгонять подсказки под фикстуры. Контроль из четырёх заведомо чужих задач
  проходится любым расширением; близкие промахи («фотосессия», «расшифровать
  запись», «смонтировать из отснятого») — вот что его держит.
* Заводить подсказку, которую держит соседняя: `говорящ` перекрывался
  `аватар`, `съёмок нет` целиком содержало `съёмок не`. Мутант молчит, и это
  тот же ненаблюдаемый заслон, что уже был оплачен на `portal_license`.
* Писать имя операции в фикстуру по памяти: `перевод_речи` вместо `дубляж`
  тихо раздуло измеренную дыру на три брифа. Теперь имена сверяются со
  словарём планировщика, и опечатка — третий исход.

**СЛЕДУЮЩИЙ ШАГ, НЕ СДЕЛАННЫЙ.** Оставшиеся 7 «не смогли» словами не
закрываются: «ведущий зачитывает сводку» подразумевает озвучку и генерацию
видео, ни разу их не называя. Нужно ЗАМЫКАНИЕ ПЛАНА ПО НЕХВАТКЕ: если шаг
требует артефакт, которого у заказчика нет и никто не производит, шаг-
производитель добавляется — и в плане помечается как выведенный нами, а не
заказанный. Пометка обязательна: без неё продукт начнёт врать в ту сторону,
от которой его тут и лечили.

**Ц2.** `studio/planner.py` — модуль агента-планировщика, его хэндоф в архиве,
писателя на ветке нет. Правку взял на себя осознанно: заслоны `кроме` и
подсказки о результате. Всё остальное в модуле не трогал.
